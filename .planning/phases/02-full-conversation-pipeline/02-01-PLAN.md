---
phase: 02-full-conversation-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/VoiceCards.tsx
  - src/components/TranscriptPanel.tsx
  - src/stores/voiceStore.ts
  - src/stores/transcriptStore.ts
autonomous: true
requirements:
  - TTS-03
  - UI-03
user_setup: []

must_haves:
  truths:
    - User can select from 4-6 voice options displayed as cards
    - Each voice card shows name, engine, and preview button
    - User can set a default voice that persists
    - User can see full conversation history with timestamps
    - User messages and AI responses are visually distinct
  artifacts:
    - path: src/components/VoiceCards.tsx
      provides: Voice card grid with preview, selection, default handling
      exports: VoiceCards component, Voice type
    - path: src/components/TranscriptPanel.tsx
      provides: Scrollable conversation history with timestamps
      exports: TranscriptPanel component, Message type
    - path: src/stores/voiceStore.ts
      provides: Voice selection state, default voice persistence
      exports: useVoiceStore hook
    - path: src/stores/transcriptStore.ts
      provides: Conversation history state management
      exports: useTranscriptStore hook, addMessage, clearTranscript
  key_links:
    - from: src/components/VoiceCards.tsx
      to: backend/services/tts_service.py
      via: API endpoint /api/tts/voices
      pattern: get_available_voices
    - from: src/components/TranscriptPanel.tsx
      to: VoiceButton.tsx
      via: WebSocket messages
      pattern: transcription, agent_response
---

<objective>
Create frontend UI components for voice selection and transcript display.

Purpose: Enable users to select from multiple TTS voices and view conversation history with timestamps.
Output: VoiceCards component, TranscriptPanel component, Zustand stores
</objective>

<execution_context>
@C:/Users/Sanjay/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-core-audio-pipeline/phase1-SUMMARY.md
@.planning/phases/02-full-conversation-pipeline/02-CONTEXT.md
@backend/services/tts_service.py
@src/components/VoiceSelector.tsx
@src/components/DictationDisplay.tsx

# Key interfaces for implementation:

From tts_service.py (AVAILABLE_VOICES):
```python
AVAILABLE_VOICES = {
    "qwen3": ["qwen3-tts-0.6b", "qwen3-tts-1.7b"],
    "neutts-air": ["neutts-air-en", "neutts-air-multi"],
    "neutts-nano": ["neutts-nano-en", "neutts-nano-hi"],
    "pocket": ["pocket-tts-en", "pocket-tts-hi"],
    "kokoro": ["kokoro-zh", "kokoro-en"],
    "edge": ["en-US-AriaNeural", "en-US-GuyNeural", "en-GB-SoniaNeural"],
}
```

From VoiceSelector.tsx (Voice interface):
```typescript
interface Voice {
  id: string;
  name: string;
  engine: string;
  is_default?: boolean;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Voice Store</name>
  <files>src/stores/voiceStore.ts</files>
  <action>
Create Zustand store for voice selection state management:
- State: selectedVoice (string), defaultVoice (string), voices (Voice[])
- Actions: setSelectedVoice, setDefaultVoice, loadVoices
- Persist defaultVoice to localStorage
- Map tts_service AVAILABLE_VOICES to Voice[] with engine info
  </action>
  <verify>
    <automated>tsc --noEmit src/stores/voiceStore.ts 2>&1 | head -20</automated>
  </verify>
  <done>Voice store created with persist middleware, selectedVoice and defaultVoice state</done>
</task>

<task type="auto">
  <name>Task 2: Create Transcript Store</name>
  <files>src/stores/transcriptStore.ts</files>
  <action>
Create Zustand store for conversation history:
- State: messages (Array of {id, role: 'user'|'assistant', content, timestamp})
- Actions: addMessage(role, content), clearTranscript
- Timestamp should be Date.now() on message creation
- Max 20 messages stored (FIFO removal)
  </action>
  <verify>
    <automated>tsc --noEmit src/stores/transcriptStore.ts 2>&1 | head -20</automated>
  </verify>
  <done>Transcript store created with addMessage and clearTranscript actions</done>
</task>

<task type="auto">
  <name>Task 3: Build Voice Cards Component</name>
  <files>src/components/VoiceCards.tsx</files>
  <action>
Create VoiceCards component displaying 4-6 voices as cards:
- Grid layout: 2 columns on mobile, 3 on desktop
- Each card shows: voice name, engine badge (e.g., "Qwen3", "Kokoro"), preview button
- Click card to select voice
- Default voice has visual indicator (border highlight)
- Preview button plays 3-second sample using TTS service
- Use existing UI theme from VoiceSelector.tsx (sci-fi terminal aesthetic)
- Connect to voiceStore for selection state
  </action>
  <verify>
    <automated>npm run build -- --filter=src/components/VoiceCards.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>VoiceCards renders 6 voice options in grid, selection works, preview plays audio</done>
</task>

<task type="auto">
  <name>Task 4: Build Transcript Panel Component</name>
  <files>src/components/TranscriptPanel.tsx</files>
  <action>
Create TranscriptPanel component for conversation history:
- Scrollable container with max-height
- Each message shows: role icon (user/AI), content, timestamp (HH:MM format)
- User messages: right-aligned, secondary color
- AI messages: left-aligned, primary color
- Auto-scroll to latest message on new content
- Empty state: "Conversation will appear here"
- Use transcriptStore for state
- Apply sci-fi terminal aesthetic from DictationDisplay.tsx
  </action>
  <verify>
    <automated>npm run build -- --filter=src/components/TranscriptPanel.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>TranscriptPanel shows messages with timestamps, scrolls to latest, distinguishes user/AI</done>
</task>

</tasks>

<verification>
Run TypeScript checks and build to verify components compile correctly.
</verification>

<success_criteria>
- VoiceCards displays 4-6 voice options with engine info
- Voice selection persists to localStorage as default
- Preview button plays sample audio
- TranscriptPanel shows conversation history with timestamps
- User and AI messages are visually distinct
- All components follow existing sci-fi terminal aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/02-full-conversation-pipeline/02-01-SUMMARY.md`
</output>
