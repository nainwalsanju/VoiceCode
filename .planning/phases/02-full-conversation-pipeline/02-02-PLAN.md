---
phase: 02-full-conversation-pipeline
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - backend/routes/agent_stream.py
  - backend/services/llm_service.py
  - backend/config/settings.py
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-04
user_setup:
  - service: NVIDIA API
    why: "LLM processing via NVIDIA NIM"
    env_vars:
      - name: NVIDIA_API_KEY
        source: "NVIDIA NGC Catalog -> API Keys"
      - name: NVIDIA_BASE_URL
        source: "NVIDIA NIM endpoint (default: https://integrate.api.nvidia.com)"

must_haves:
  truths:
    - Transcribed text is sent to NVIDIA API for processing
    - LLM response streams token-by-token to TTS
    - Streaming happens sentence-by-sentence (not full response)
    - Fallback message shown when LLM unavailable
  artifacts:
    - path: backend/services/llm_service.py
      provides: NVIDIA API LLM client with streaming
      exports: LLMService class, stream_completion method
    - path: backend/routes/agent_stream.py
      provides: WebSocket endpoint with sentence streaming
      exports: /agent/stream WebSocket, sentence buffer logic
  key_links:
    - from: backend/routes/agent_stream.py
      to: backend/services/llm_service.py
      via: LLMService.stream_completion()
      pattern: async for chunk in stream_completion
    - from: backend/routes/agent_stream.py
      to: backend/services/tts_service.py
      via: tts_service.stream_audio_async()
      pattern: stream audio on sentence completion
---

<objective>
Integrate NVIDIA API for LLM processing with sentence-by-sentence streaming to TTS.

Purpose: Replace stub response with real AI responses, stream tokens as they're generated.
Output: LLM service, updated agent_stream endpoint with streaming
</objective>

<execution_context>
@C:/Users/Sanjay/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@backend/routes/agent_stream.py
@backend/services/tts_service.py
@.planning/phases/02-full-conversation-pipeline/02-CONTEXT.md

# NVIDIA API OpenAI-compatible endpoint:
# Base URL: https://integrate.api.nvidia.com/v1
# Model: nvidia/llama-3.1-nemotron-70b-instruct (or similar)
# Streaming: Use openai.ChatCompletion.create with stream=True
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LLM Service</name>
  <files>backend/services/llm_service.py</files>
  <action>
Create NVIDIA API LLM service:
- Class LLMService with async methods
- stream_completion(messages: list, system_prompt: str) -> AsyncGenerator[str]
- Connect to NVIDIA NIM endpoint: https://integrate.api.nvidia.com/v1/chat/completions
- Use OpenAI client with azure AD-style authentication (api_key in header)
- Stream response tokens via Server-Sent Events
- Handle errors: return fallback message on API failure
- Configuration via environment: NVIDIA_API_KEY, NVIDIA_BASE_URL, NVIDIA_MODEL
  </action>
  <verify>
    <automated>python -c "from backend.services.llm_service import LLMService; print('OK')" 2>&1</automated>
  </verify>
  <done>LLM service created with streaming support, error handling</done>
</task>

<task type="auto">
  <name>Task 2: Implement Sentence Buffer for Streaming</name>
  <files>backend/routes/agent_stream.py</files>
  <action>
Modify agent_stream.py to stream sentence-by-sentence:
- Add sentence_buffer: str to track incomplete sentences
- On each token received from LLM:
  - Append to buffer
  - Check if buffer ends with sentence terminator (.!?)
  - If sentence complete: emit to TTS, clear buffer
- Handle partial tokens gracefully
- Emit remaining buffer content on stream end
- Send "sentence_start" and "sentence_end" events to frontend
  </action>
  <verify>
    <automated>python -c "from backend.routes.agent_stream import router; print('OK')" 2>&1</automated>
  </verify>
  <done>Sentence buffer implemented, tokens stream on sentence boundaries</done>
</task>

<task type="auto">
  <name>Task 3: Integrate LLM with Agent Stream</name>
  <files>backend/routes/agent_stream.py</files>
  <action>
Wire LLM service into agent_stream WebSocket:
- Replace stub response with LLMService.stream_completion()
- Pass conversation context (last 10 messages) to LLM
- On each complete sentence: stream to TTS immediately
- Send "llm_start" and "llm_end" events
- Add error handling: if LLM fails, send fallback message
- Maintain context_buffer for conversation history
  </action>
  <verify>
    <automated>python -c "from backend.routes.agent_stream import router; print('OK')" 2>&1</automated>
  </verify>
  <done>LLM integrated, sentence-by-sentence streaming to TTS works</done>
</task>

</tasks>

<verification>
Test LLM service imports and agent_stream endpoint loads without errors.
</verification>

<success_criteria>
- LLM service connects to NVIDIA API successfully
- Tokens stream sentence-by-sentence (not full response)
- TTS plays audio as sentences complete
- Fallback message displays when API unavailable
- Context maintained across messages (last 10 exchanges)
</success_criteria>

<output>
After completion, create `.planning/phases/02-full-conversation-pipeline/02-02-SUMMARY.md`
</output>
