---
phase: 02-full-conversation-pipeline
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/components/VoiceButton.tsx
  - src/stores/sessionStore.ts
  - backend/routes/agent_stream.py
autonomous: true
requirements:
  - AGENT-03
  - CONV-02
  - CONV-03
user_setup: []

must_haves:
  truths:
    - Session maintains conversation context (last 10 message exchanges)
    - State transitions smoothly: IDLE → LISTENING → PROCESSING → SPEAKING → IDLE
    - Continuous conversation without manual re-triggering
    - Frontend reflects current session state visually
  artifacts:
    - path: src/stores/sessionStore.ts
      provides: Session state machine
      exports: SessionState enum, useSessionStore hook
    - path: src/components/VoiceButton.tsx
      provides: Updated voice button with state indicators
      exports: VoiceButton component with state display
  key_links:
    - from: src/components/VoiceButton.tsx
      to: backend/routes/agent_stream.py
      via: WebSocket messages
      pattern: listening, processing, speaking, idle
    - from: src/stores/sessionStore.ts
      to: src/stores/transcriptStore.ts
      via: Zustand
      pattern: addMessage on agent_response
---

<objective>
Implement session state management for continuous conversation flow.

Purpose: Maintain conversation context and handle state transitions smoothly.
Output: Session store, updated VoiceButton with state indicators
</objective>

<execution_context>
@C:/Users/Sanjay/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/VoiceButton.tsx
@backend/routes/agent_stream.py
@src/stores/transcriptStore.ts

# Existing state machine from VoiceButton (Phase 1):
# States: idle, connecting, listening, speaking, error
# Need to extend for Phase 2: processing (LLM thinking)
</context>

<tasks>

<task type="auto">
<name>Task 1: Create Session Store</name>
<files>src/stores/sessionStore.ts</files>
<action>
Create Zustand store for session state management:
- Define states: IDLE, LISTENING, PROCESSING, SPEAKING (matches CONV-03)
- State: currentState, isContinuousMode, contextLength
- Actions:
  - setState(newState): Validates transitions
  - startListening(): IDLE → LISTENING
  - startProcessing(): LISTENING → PROCESSING
  - startSpeaking(): PROCESSING → SPEAKING
  - endSpeaking(): Any → IDLE
- Emit state changes to WebSocket for backend sync
  </action>
  <verify>
    <automated>tsc --noEmit src/stores/sessionStore.ts 2>&1 | head -20</automated>
  </verify>
  <done>Session store created with state machine logic</done>
</task>

<task type="auto">
<name>Task 2: Wire Session State to WebSocket</name>
<files>backend/routes/agent_stream.py</files>
<action>
Update agent_stream to send state messages:
- On transcription start: send {"type": "state", "state": "listening"}
- On LLM processing: send {"type": "state", "state": "processing"}
- On TTS start: send {"type": "state", "state": "speaking"}
- On complete: send {"type": "state", "state": "idle"}
- This enables continuous conversation flow
  </action>
  <verify>
    <automated>python -c "from backend.routes.agent_stream import router; print('OK')" 2>&1</automated>
  </verify>
  <done>Backend sends state messages to frontend</done>
</task>

<task type="auto">
<name>Task 3: Update VoiceButton with State Display</name>
<files>src/components/VoiceButton.tsx</files>
  <action>
Update VoiceButton to display session states:
- Listen to WebSocket state messages
- Update visual indicator based on current state:
  - LISTENING: pulsing blue (microphone active)
  - PROCESSING: spinning/thinking indicator (LLM working)
  - SPEAKING: pulsing green (TTS playing)
- Show state label text
- Handle continuous mode: auto-return to LISTENING after SPEAKING
- Connect to sessionStore for state management
  </action>
  <verify>
    <automated>npm run build -- --filter=src/components/VoiceButton.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>VoiceButton shows state transitions, continuous mode works</done>
</task>

</tasks>

<verification>
Build frontend and verify backend loads without errors.
</verification>

<success_criteria>
- Session state machine transitions correctly: IDLE → LISTENING → PROCESSING → SPEAKING → IDLE
- VoiceButton displays current state visually
- Continuous conversation works without re-triggering
- Backend sends state messages to frontend
</success_criteria>

<output>
After completion, create `.planning/phases/02-full-conversation-pipeline/02-03-SUMMARY.md`
</output>
