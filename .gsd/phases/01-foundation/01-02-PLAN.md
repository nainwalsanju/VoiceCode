---
phase: 01-foundation
plan: 02
type: auto
---

<objective>
Set up Python FastAPI backend with logging and error handling.

Purpose: Create the backend service that will run Pocket TTS and STT.
Output: FastAPI server that can handle requests and stream responses.
</objective>

<context>
@PROJECT.md
@ROADMAP.md
@01-01-PLAN.md
</context>

<tasks>
  <task type="auto">
    <name>Create Python backend structure</name>
    <action>
      1. Create backend/ directory in project root
      2. Create backend/main.py - FastAPI entry point
      3. Create backend/requirements.txt with dependencies
      4. Create backend/.env.example for configuration
    </action>
    <verify>
      - backend/ directory exists
      - backend/main.py exists
      - backend/requirements.txt exists
    </verify>
    <done>Backend structure created</done>
  </task>

  <task type="auto">
    <name>Set up FastAPI application</name>
    <action>
      1. Create basic FastAPI app with CORS
      2. Add health check endpoint /health
      3. Configure logging with structlog
      4. Add global exception handler
    </action>
    <verify>
      - GET /health returns 200
      - Logs appear in console
      - CORS configured for frontend
    </verify>
    <done>FastAPI app configured</done>
  </task>

  <task type="auto">
    <name>Create TTS routes</name>
    <action>
      1. Create backend/routes/tts.py
      2. Add POST /tts/generate endpoint
      3. Add POST /tts/stream endpoint (streaming response)
      4. Create route module and import in main.py
    </action>
    <verify>
      - TTS routes module exists
      - Endpoints registered in app
    </verify>
    <done>TTS routes created</done>
  </task>

  <task type="auto">
    <name>Test backend startup</name>
    <action>
      1. Create Python virtual environment: python3 -m venv venv
      2. Activate: source venv/bin/activate
      3. Install deps: pip install -r backend/requirements.txt
      4. Run backend: cd backend && uvicorn main:app --reload --port 8000
    </action>
    <verify>
      - Server starts without errors
      - /health endpoint responds
      - No import errors
    </verify>
    <done>Backend runs successfully</done>
  </task>
</tasks>

<success_criteria>
- FastAPI server starts on port 8000
- Health check endpoint returns 200
- TTS endpoints respond (even if not implemented yet)
- Logs show startup messages
</success_criteria>
