---
phase: 04-voice-commands
plan: 03
type: auto
---

<objective>
Build UI-based command builder.

Purpose: Allow users to create commands without coding.
Output: Complete command builder interface.
</objective>

<context>
@PROJECT.md
@ROADMAP.md
@04-01-PLAN.md
</context>

<tasks>
  <task type="auto">
    <name>Create command types</name>
    <action>
      1. Create src/types/voiceCommand.ts
      2. Define VoiceCommand interface
      3. Add action type definitions
    </action>
    <verify>
      - TypeScript types defined
    </verify>
    <done>Command types created</done>
  </task>

  <task type="auto">
    <name>Create command API client</name>
    <action>
      1. Create src/api/commands.ts
      2. Add CRUD functions
      3. Add execute function
    </action>
    <verify>
      - API client exists
    </verify>
    <done>API client created</done>
  </task>

  <task type="auto">
    <name>Build command list UI</name>
    <action>
      1. Create src/components/CommandList.tsx
      2. Display all commands
      3. Enable/disable toggle
      4. Edit and delete actions
    </action>
    <verify>
      - Commands displayed
      - CRUD actions work
    </verify>
    <done>Command list created</done>
  </task>

  <task type="auto">
    <name>Build command form</name>
    <action>
      1. Create src/components/CommandForm.tsx
      2. Trigger phrase input
      3. Action type selector
      4. Action config fields (dynamic based on type)
      5. Category selector
      6. Save button
    </action>
    <verify>
      - Can create new command
      - All action types configurable
    </verify>
    <done>Command form created</done>
  </task>
</tasks>

<success_criteria>
- Command list displays all commands
- Can create new command
- Can edit existing command
- Can delete command
- Can enable/disable commands
</success_criteria>
