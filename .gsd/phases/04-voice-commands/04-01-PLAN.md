---
phase: 04-voice-commands
plan: 01
type: auto
---

<objective>
Define voice command data model and storage.

Purpose: Establish the structure for voice commands.
Output: Command schema and storage implementation.
</objective>

<context>
@PROJECT.md
@ROADMAP.md
</context>

<tasks>
  <task type="auto">
    <name>Define voice command models</name>
    <action>
      1. Create backend/models/voice_command.py
      2. Define VoiceCommand Pydantic schema
      3. Fields: id, trigger, action_type, action_config, enabled, category, created_at
      4. Action types: insert_text, run_command, hotkey, snippet
    </action>
    <verify>
      - VoiceCommand model exists
      - All action types defined
    </verify>
    <done>Command models defined</done>
  </task>

  <task type="auto">
    <name>Set up command storage</name>
    <action>
      1. Create data/commands/ directory
      2. Implement JSON file-based storage
      3. Add CRUD operations
    </action>
    <verify>
      - Commands persist to disk
      - CRUD operations work
    </verify>
    <done>Command storage set up</done>
  </task>

  <task type="auto">
    <name>Create command API routes</name>
    <action>
      1. Create backend/routes/commands.py
      2. GET /commands - list all
      3. POST /commands - create
      4. PUT /commands/:id - update
      5. DELETE /commands/:id - delete
    </action>
    <verify>
      - All CRUD endpoints work
    </verify>
    <done>Command API routes created</done>
  </task>
</tasks>

<success_criteria>
- VoiceCommand model defined
- Commands persist to disk
- CRUD endpoints functional
</success_criteria>
