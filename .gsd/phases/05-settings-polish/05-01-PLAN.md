---
phase: 05-settings-polish
plan: 01
type: auto
---

<objective>
Build settings panel UI.

Purpose: Allow users to configure app behavior.
Output: Complete settings interface.
</objective>

<context>
@PROJECT.md
@ROADMAP.md
</context>

<tasks>
  <task type="auto">
    <name>Create settings data model</name>
    <action>
      1. Create backend/models/settings.py
      2. Define AppSettings Pydantic schema
      3. Fields: stt_provider, tts_voice, tts_speed, microphone, hotkeys, auto_start, tts_enabled
    </action>
    <verify>
      - Settings model exists
    </verify>
    <done>Settings model created</done>
  </task>

  <task type="auto">
    <name>Create settings storage</name>
    <action>
      1. Create data/settings.json
      2. Implement get/set in backend
      3. Add default values
    </action>
    <verify>
      - Settings persist to file
      - Defaults applied on first run
    </verify>
    <done>Settings storage created</done>
  </task>

  <task type="auto">
    <name>Create settings API</name>
    <action>
      1. Add GET /settings endpoint
      2. Add PUT /settings endpoint
      3. Add GET /settings/microphones - list available mics
    </action>
    <verify>
      - Settings endpoints work
    </verify>
    <done>Settings API created</done>
  </task>

  <task type="auto">
    <name>Build settings UI</name>
    <action>
      1. Create src/components/SettingsPanel.tsx
      2. STT provider selector (local/cloud)
      3. TTS voice and speed controls
      4. Microphone selector
      5. Hotkey configuration
      6. Toggle switches
    </action>
    <verify>
      - Settings panel displays
      - Can modify settings
      - Settings persist
    </verify>
    <done>Settings UI built</done>
  </task>
</tasks>

<success_criteria>
- Settings persist between sessions
- All settings configurable via UI
- Default values applied on first run
</success_criteria>
