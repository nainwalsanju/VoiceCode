---
phase: 05-settings-polish
plan: 02
type: auto
---

<objective>
Implement system tray and window management.

Purpose: Enable background operation and system integration.
Output: App runs in system tray with proper window controls.
</objective>

<context>
@PROJECT.md
@ROADMAP.md
@05-01-PLAN.md
</context>

<tasks>
  <task type="auto">
    <name>Add system tray support</name>
    <action>
      1. Update src-tauri/tauri.conf.json
      2. Add tray plugin or native implementation
      3. Create tray menu (show/hide, quit)
      4. Add tray icon
    </action>
    <verify>
      - Tray icon appears
      - Menu works
    </verify>
    <done>System tray added</done>
  </task>

  <task type="auto">
    <name>Configure window settings</name>
    <action>
      1. Set default window size and position
      2. Add minimize to tray option
      3. Add always on top option
      4. Implement proper close behavior
    </action>
    <verify>
      - Window settings work
      - Minimize to tray works
    </verify>
    <done>Window settings configured</done>
  </task>

  <task type="auto">
    <name>Add global hotkey support</name>
    <action>
      1. Add global hotkey plugin
      2. Register voice activation hotkey
      3. Allow configurable hotkeys
      4. Show hotkey in settings
    </action>
    <verify>
      - Global hotkeys work
      - Works when app in background
    </verify>
    <done>Global hotkeys added</done>
  </task>
</tasks>

<success_criteria>
- System tray icon with menu
- Window minimize/close behavior
- Global hotkeys work
</success_criteria>
