---
phase: 03-voice-cloning
plan: 04
type: auto
---

<objective>
Integrate voice selection into TTS output.

Purpose: Allow users to select which voice to use for TTS.
Output: Voice selector in UI, TTS uses selected voice.
</objective>

<context>
@PROJECT.md
@ROADMAP.md
@03-02-PLAN.md
</context>

<tasks>
  <task type="auto">
    <name>Add voice selection to TTS endpoint</name>
    <action>
      1. Update POST /tts/generate to accept voice_id
      2. Look up voice profile by ID
      3. Use voice embedding in TTS generation
      4. Handle case when voice not found
    </action>
    <verify>
      - TTS generates with selected voice
    </verify>
    <done>Voice selection in TTS</done>
  </task>

  <task type="auto">
    <name>Create voice selector component</name>
    <action>
      1. Create src/components/VoiceSelector.tsx
      2. Dropdown with available voices
      3. Show current selection
      4. Preview voice button
    </action>
    <verify>
      - Voice selector UI exists
      - Shows all available voices
    </verify>
    <done>Voice selector created</done>
  </task>

  <task type="auto">
    <name>Test complete voice cloning flow</name>
    <action>
      1. Upload audio sample
      2. Create voice profile
      3. Select voice in TTS
      4. Generate and play TTS
    </verify>
    <done>Complete flow working</done>
  </task>
</tasks>

<success_criteria>
- Voice selector shows all profiles
- TTS uses selected voice
- Preview plays selected voice
- Default voice used on startup
</success_criteria>
