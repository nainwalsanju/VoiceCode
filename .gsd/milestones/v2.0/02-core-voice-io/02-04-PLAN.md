---
phase: 02-core-voice-io
plan: 04
type: auto
---

<objective>
Create complete audio pipeline: capture → STT → display.

Purpose: Connect voice input to transcription display for dictation workflow.
Output: User can speak and see transcribed text in real-time.
</objective>

<context>
@PROJECT.md
@ROADMAP.md
@02-02-PLAN.md
@02-03-PLAN.md
</context>

<tasks>
  <task type="auto">
    <name>Create audio capture on frontend</name>
    <action>
      1. Create src/hooks/useAudioCapture.ts
      2. Implement microphone access using Web Audio API
      3. Add start/stop recording methods
      4. Handle permissions and errors
    </action>
    <verify>
      - Audio capture hook exists
      - Can access microphone
    </verify>
    <done>Audio capture created</done>
  </task>

  <task type="auto">
    <name>Create real-time transcription service</name>
    <action>
      1. Create backend/routes/stt_stream.py
      2. Implement WebSocket /stt/stream endpoint
      3. Add chunked audio transcription
      4. Return partial results in real-time
    </action>
    <verify>
      - WebSocket endpoint exists
      - Streams transcriptions
    </verify>
    <done>Real-time STT created</done>
  </task>

  <task type="auto">
    <name>Build dictation UI component</name>
    <action>
      1. Create src/components/DictationDisplay.tsx
      2. Add large text display area
      3. Add edit capability for corrections
      4. Add copy/clear buttons
    </action>
    <verify>
      - DictationDisplay component exists
      - Shows transcribed text
    </verify>
    <done>Dictation UI created</done>
  </task>

  <task type="auto">
    <name>Integrate voice button with audio pipeline</name>
    <action>
      1. Create src/components/VoiceButton.tsx
      2. States: idle, listening, processing
      3. Connect to audio capture and STT
      4. Show visual feedback during recording
    </action>
    <verify>
      - Voice button component works
      - Shows correct states
      - Triggers transcription
    </verify>
    <done>Voice button integrated</done>
  </task>

  <task type="auto">
    <name>End-to-end dictation test</name>
    <action>
      1. Start backend server
      2. Start frontend dev server
      3. Click voice button and speak
      4. Verify text appears in display
    </action>
    <verify>
      - Complete dictation flow works
      - Text displays accurately
    </verify>
    <done>End-to-end test passed</done>
  </task>
</tasks>

<success_criteria>
- VoiceButton triggers audio capture
- Audio streams to backend STT
- Transcribed text displays in real-time
- User can edit/correct transcribed text
- Visual feedback shows recording state
</success_criteria>
