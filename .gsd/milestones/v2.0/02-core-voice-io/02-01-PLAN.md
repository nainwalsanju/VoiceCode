---
phase: 02-core-voice-io
plan: 01
type: auto
---

<objective>
Integrate Pocket TTS with Python backend for local text-to-speech.

Purpose: Enable local TTS generation using Pocket TTS (CPU-based, no GPU required).
Output: Backend can generate speech from text using Pocket TTS.
</objective>

<context>
@PROJECT.md
@ROADMAP.md
</context>

<tasks>
  <task type="auto">
    <name>Install Pocket TTS</name>
    <action>
      1. Add pocket-tts to backend/requirements.txt
      2. Run: pip install pocket-tts
      3. Verify installation: python -c "from pocket_tts import TTSModel; print('OK')"
    </action>
    <verify>
      - pocket-tts package installed
      - Import test passes
    </verify>
    <done>Pocket TTS installed</done>
  </task>

  <task type="auto">
    <name>Create TTS service module</name>
    <action>
      1. Create backend/services/tts_service.py
      2. Implement TTSModel wrapper class
      3. Add methods: load_model(), generate_audio(), get_available_voices()
      4. Add voice caching for performance
    </action>
    <verify>
      - TTS service module created
      - Can import and use TTSModel
    </verify>
    <done>TTS service module created</done>
  </task>

  <task type="auto">
    <name>Implement TTS generation endpoint</name>
    <action>
      1. Update backend/routes/tts.py
      2. Implement POST /tts/generate
      3. Accept JSON: { text, voice, speed }
      4. Return audio bytes (WAV format)
      5. Add error handling for empty text
    </action>
    <verify>
      - POST /tts/generate returns audio
      - Audio is valid WAV format
      - Error handling works
    </verify>
    <done>TTS generation endpoint implemented</done>
  </task>

  <task type="auto">
    <name>Test TTS generation</name>
    <action>
      1. Start backend server
      2. Test with curl: curl -X POST http://localhost:8000/tts/generate -H "Content-Type: application/json" -d '{"text":"Hello world","voice":"alba"}'
      3. Verify audio is returned
      4. Save and play audio file to verify quality
    </action>
    <verify>
      - TTS generates audio successfully
      - Audio plays correctly
    </verify>
    <done>TTS generation working</done>
  </task>
</tasks>

<success_criteria>
- Pocket TTS installed and importable
- /tts/generate endpoint returns valid WAV audio
- Can generate speech with different voices
- Model loads and generates audio without errors
</success_criteria>
