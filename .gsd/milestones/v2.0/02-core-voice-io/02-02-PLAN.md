---
phase: 02-core-voice-io
plan: 02
type: auto
---

<objective>
Implement streaming TTS for real-time audio playback.

Purpose: Enable low-latency TTS streaming (~200ms first chunk) for responsive audio feedback.
Output: Frontend can stream and play TTS audio in real-time.
</objective>

<context>
@PROJECT.md
@ROADMAP.md
@02-01-PLAN.md
</context>

<tasks>
  <task type="auto">
    <name>Add streaming support to TTS service</name>
    <action>
      1. Update backend/services/tts_service.py
      2. Add stream_audio() method that yields chunks
      3. Implement chunk-based audio generation
      4. Add chunk size configuration
    </action>
    <verify>
      - TTS service has streaming method
      - Method yields audio chunks
    </verify>
    <done>Streaming support added</done>
  </task>

  <task type="auto">
    <name>Create streaming TTS endpoint</name>
    <action>
      1. Update backend/routes/tts.py
      2. Add POST /tts/stream endpoint
      3. Use StreamingResponse with async generator
      4. Set proper media type for audio streaming
    </action>
    <verify>
      - /tts/stream endpoint exists
      - Returns streaming response
    </verify>
    <done>Streaming endpoint created</endpoint>
  </task>

  <task type="auto">
    <name>Create frontend audio service</name>
    <action>
      1. Create src/services/audioService.ts
      2. Implement playAudio() for playing audio blobs
      3. Add streamAudio() for streaming playback
      4. Add stopAudio() to cancel playback
      5. Handle audio state (playing, paused, stopped)
    </action>
    <verify>
      - Audio service module exists
      - Has play, stream, stop methods
    </verify>
    <done>Audio service created</done>
  </task>

  <task type="auto">
    <name>Test streaming audio playback</name>
    <action>
      1. Start backend server
      2. Call /tts/stream from frontend
      3. Verify audio plays with minimal latency
      4. Test stop/cancel functionality
    </action>
    <verify>
      - Audio streams and plays
      - Latency under 500ms
      - Stop works correctly
    </verify>
    <done>Streaming playback working</done>
  </task>
</tasks>

<success_criteria>
- /tts/stream returns streaming audio
- Frontend can play streamed audio
- Latency under 500ms for first chunk
- Audio can be stopped mid-playback
</success_criteria>
