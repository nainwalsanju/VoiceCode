---
phase: 04-voice-commands
plan: 02
type: auto
---

<objective>
Implement command execution engine.

Purpose: Parse voice input and execute matching commands.
Output: Backend can match triggers to actions and execute them.
</objective>

<context>
@PROJECT.md
@ROADMAP.md
@04-01-PLAN.md
</context>

<tasks>
  <task type="auto">
    <name>Create command executor service</name>
    <action>
      1. Create backend/services/command_executor.py
      2. Implement CommandExecutor class
      3. Add match_trigger() method
      4. Add execute_command() method with action type routing
    </action>
    <verify>
      - Executor can match triggers
      - Can route to correct action type
    </verify>
    <done>Command executor created</done>
  </task>

  <task type="auto">
    <name>Implement action handlers</name>
    <action>
      1. Add insert_text handler - returns text to insert
      2. Add run_command handler - executes shell command
      3. Add hotkey handler - simulates keyboard shortcut
      4. Add snippet handler - expands code snippet
    </action>
    <verify>
      - All action types handled
      - Correct output for each type
    </verify>
    <done>Action handlers implemented</done>
  </task>

  <task type="auto">
    <name>Create command execution endpoint</name>
    <action>
      1. Add POST /commands/execute endpoint
      2. Accept { text, context }
      3. Match trigger and execute
      4. Return action result
    </action>
    <verify>
      - Endpoint matches and executes commands
      - Returns correct result
    </verify>
    <done>Execute endpoint created</done>
  </task>
</tasks>

<success_criteria>
- Commands can be matched by trigger
- Actions execute correctly
- Results returned properly
</success_criteria>
