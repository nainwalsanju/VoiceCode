---
phase: 04-voice-commands
plan: 04
type: auto
---

<objective>
Add command templates and testing interface.

Purpose: Provide starter commands and allow testing before activation.
Output: Template library and command test UI.
</objective>

<context>
@PROJECT.md
@ROADMAP.md
@04-03-PLAN.md
</context>

<tasks>
  <task type="auto">
    <name>Create command template library</name>
    <action>
      1. Create data/commands/templates.json
      2. Add navigation commands (go to line, open file)
      3. Add editing commands (delete line, copy line)
      4. Add code generation commands (function, class, loop)
    </action>
    <verify>
      - Templates JSON exists
      - At least 10 templates
    </verify>
    <done>Templates created</done>
  </task>

  <task type="auto">
    <name>Create template API endpoint</name>
    <action>
      1. Add GET /commands/templates endpoint
      2. Return available templates
      3. Add POST /commands/templates/:id/use - import template
    </action>
    <verify>
      - Templates endpoint works
    </verify>
    <done>Template API created</done>
  </task>

  <task type="auto">
    <name>Build command test interface</name>
    <action>
      1. Create src/components/CommandTester.tsx
      2. Text input for test phrase
      3. Execute button
      4. Result display
    </action>
    <verify>
      - Can test commands
      - Results displayed
    </verify>
    <done>Test interface created</done>
  </task>

  <task type="auto">
    <name>Integrate commands with dictation</name>
    <action>
      1. Update dictation flow to check commands
      2. If command matched, execute action
      3. If no match, return as plain text
      4. Add configuration for command prefix
    </action>
    <verify>
      - Commands integrate with dictation
      - Non-command text returns normally
    </verify>
    <done>Commands integrated</done>
  </task>
</tasks>

<success_criteria>
- Command templates available
- Can import templates
- Can test commands
- Commands work in dictation flow
</success_criteria>
