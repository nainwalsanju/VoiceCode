---
phase: 05-settings-polish
plan: 04
type: auto
---

<objective>
Final UI polish and testing.

Purpose: Polish UI and ensure everything works together.
Output: Production-ready app.
</objective>

<context>
@PROJECT.md
@ROADMAP.md
</context>

<tasks>
  <task type="auto">
    <name>UI polish and styling</name>
    <action>
      1. Review all components match design spec
      2. Add loading states
      3. Add error states
      4. Add animations for transitions
      5. Ensure responsive behavior
    </action>
    <verify>
      - All states handled
      - Animations smooth
    </verify>
    <done>UI polished</done>
  </task>

  <task type="auto">
    <name>End-to-end testing</name>
    <action>
      1. Test complete dictation flow
      2. Test voice cloning flow
      3. Test command creation and execution
      4. Test settings persistence
      5. Test system tray integration
    </action>
    <verify>
      - All features work
      - No critical bugs
    </verify>
    <done>E2E testing complete</done>
  </task>

  <task type="auto">
    <name>Build production executable</name>
    <action>
      1. Run: npm run build
      2. Run: cd src-tauri && cargo build --release
      3. Verify .exe/.app created
    </action>
    <verify>
      - Executable created
      - App runs outside dev mode
    </verify>
    <done>Production build done</done>
  </task>
</tasks>

<success_criteria>
- All features functional
- Production build succeeds
- App runs standalone
</success_criteria>
