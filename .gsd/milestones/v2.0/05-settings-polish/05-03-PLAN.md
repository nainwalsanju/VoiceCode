---
phase: 05-settings-polish
plan: 03
type: auto
---

<objective>
Add app state persistence.

Purpose: Remember user preferences and last state.
Output: App restores to previous state on restart.
</objective>

<context>
@PROJECT.md
@ROADMAP.md
@05-01-PLAN.md
</context>

<tasks>
  <task type="auto">
    <name>Implement state persistence</name>
    <action>
      1. Create state storage (data/app_state.json)
      2. Save: last used voice, window position, active commands
      3. Load state on startup
      4. Add state update on changes
    </action>
    <verify>
      - State saves on changes
      - State restores on startup
    </verify>
    <done>State persistence implemented</done>
  </task>

  <task type="auto">
    <name>Add startup behavior</name>
    <action>
      1. Add auto-start option in settings
      2. Configure Tauri for auto-start
      3. Start minimized option
    </action>
    <verify>
      - Auto-start works
      - Starts minimized option works
    </verify>
    <done>Startup behavior added</done>
  </task>
</tasks>

<success_criteria>
- App state persists between sessions
- Auto-start configurable
</success_criteria>
