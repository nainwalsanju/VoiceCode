---
phase: 01-foundation
plan: 01
type: auto
---

<objective>
Initialize Tauri 2.x desktop project with React + TypeScript frontend.

Purpose: Establish the desktop app shell with modern frontend tooling.
Output: Working Tauri app that can build and run on desktop.
</objective>

<context>
@PROJECT.md
@ROADMAP.md
</context>

<tasks>
  <task type="auto">
    <name>Check prerequisites</name>
    <action>
      1. Check if Node.js 18+ is installed: node --version
      2. Check if Rust is installed: rustc --version
      3. Check if Python 3.10+ is installed: python3 --version
      4. Verify npm or pnpm is available
    </action>
    <verify>All prerequisites installed</verify>
    <done>Prerequisites verified</done>
  </task>

  <task type="auto">
    <name>Initialize Tauri project</name>
    <action>
      1. Run: cd /Users/sanju/Projects/voicecode
      2. Run: npm create tauri-app@latest . -- --template react-ts --manager npm --yes
      3. This creates Tauri 2.x project with React + TypeScript
    </action>
    <verify>
      - src-tauri/ directory exists with Cargo.toml
      - package.json exists with dependencies
      - vite.config.ts exists
    </verify>
    <done>Tauri project initialized</done>
  </task>

  <task type="auto">
    <name>Install frontend dependencies</name>
    <action>
      1. Run: npm install
      2. Install additional deps: npm install zustand @tanstack/react-query tailwindcss postcss autoprefixer
      3. Initialize Tailwind: npx tailwindcss init -p
    </action>
    <verify>npm install completes without errors</verify>
    <done>Dependencies installed</done>
  </task>

  <task type="auto">
    <name>Configure Tailwind CSS</name>
    <action>
      1. Update tailwind.config.js with content paths
      2. Add Tailwind directives to src/index.css
      3. Configure Tailwind theme colors from PROJECT.md
    </action>
    <verify>
      - tailwind.config.js exists
      - src/index.css has Tailwind directives
    </verify>
    <done>Tailwind configured</done>
  </task>

  <task type="auto">
    <name>Verify empty shell builds</name>
    <action>
      1. Run: npm run build
      2. Run: cd src-tauri && cargo build --release
    </action>
    <verify>
      - dist/ folder generated
      - src-tauri/target/release contains executable
    </verify>
    <done>Empty shell builds successfully</done>
  </task>
</tasks>

<success_criteria>
- npm run dev starts development server
- Tauri app launches without errors
- Frontend renders basic React app
- No TypeScript or Rust compilation errors
</success_criteria>
